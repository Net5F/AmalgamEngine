#!/bin/bash

# Installs the required dependencies for doing an engine build.
# Tested on 20.0.4.

sudo apt install g++ cmake libsdl2-dev libsdl2-net-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev ninja-build patchelf
